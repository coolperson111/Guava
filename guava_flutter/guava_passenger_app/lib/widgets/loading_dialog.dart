import 'package:flutter/material.dart';

class LoadingDialog extends StatelessWidget {

  String messageText;

  const LoadingDialog({super.key, require this.messageText});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
